/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [],
    dangerouslyAllowSVG: true,
    domains: [],
    unoptimized: true,
  },
};

export default nextConfig; 